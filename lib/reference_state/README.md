# ReferenceState v2 placeholder

This directory reserves ownership for the future ReferenceState v2 runtime
infrastructure.

The planned state includes:

- `rho_ref`;
- `T_ref`;
- `P_ref`;
- gravity `g`;
- `alpha`;
- `Cp`;
- `Ks`;
- `gamma`;
- `beta`;
- reference phase fractions.

Commit 0 contains no implementation, build-system registration, runtime
configuration, or physical change.
