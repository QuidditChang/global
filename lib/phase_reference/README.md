# Phase-reference placeholder

This directory reserves ownership for the future phase-reference subsystem.

The planned state separates:

- reference equilibrium phase fraction `Xref`;
- dynamic phase fraction `X`;
- phase anomaly `deltaX = X-Xref`.

Commit 0 contains no phase implementation and does not change legacy phase
buoyancy.
